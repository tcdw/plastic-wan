# Dev-only: where the Vite dev server proxies /api requests to.
# The production AdminServer runs same-origin inside `serve`, so this is only
# used by `bun run admin:dev` (or `vite dev` in apps/admin-next).
ADMIN_API_TARGET=http://127.0.0.1:8787
