# Dev-only: where the Rsbuild dev server proxies /api requests to.
# The production AdminServer runs same-origin inside `serve`, so this is only
# used by `pnpm run admin:dev` (or `rsbuild dev` in apps/admin-next).
ADMIN_API_TARGET=http://127.0.0.1:8787
